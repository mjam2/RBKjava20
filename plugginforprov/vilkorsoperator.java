package plugginforprov;

public class vilkorsoperator {
    // uttryck_1 ? uttryck_2 : uttryck_3;
    //max = tal1 > tal 2 ? tal1 : tal2;
    /* Du kan läsa satsen så här: Om tal1 är större än tal2, tilldelas variablen max värdet hos
    tal1, annars värdet hos tal2.
     */
     /* Motsvaradende kod om en if-else-sats används
     if (tal>tal2)
     max = tal1;
     else
     max = tal2
      */
}
